# Copy to .env and fill in. NEVER commit .env (it is gitignored).
# The default `fake` provider needs NO keys — these are only for real LLM runs.

# DeepSeek (OpenAI-compatible endpoint). Used with --provider deepseek.
DEEPSEEK_API_KEY=

# Anthropic Claude. Used with --provider claude.
ANTHROPIC_API_KEY=
